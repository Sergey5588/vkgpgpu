#version 450
layout(local_size_x = 256) in;
layout(set=0, binding=0) buffer A {
	uint a[];
};
layout(set=0, binding=1) buffer B {
	uint b[];
};

layout(push_constant) uniform constants {
	uint alpha;
} Consts;
void main() {
	uint i = gl_GlobalInvocationID.x;
	if(i>=1337) return;
	b[i] = a[i]*Consts.alpha;
}
